---
const slug = Astro.params.slug;
if (slug && !Array.isArray(slug)) {
	return Astro.redirect(`/recent-matters-and-blogs/${slug}/`);
}
return new Response(null, { status: 404, statusText: 'Not Found' });
---
